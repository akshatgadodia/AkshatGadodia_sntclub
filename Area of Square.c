#include<stdio.h>

int main() {
   int side, area;
   printf("Enter the Length of Side  \n");
   scanf("%d", &side);
   area = side * side;
   printf("Area of Square is %d \n", area);

   return (0);
}
